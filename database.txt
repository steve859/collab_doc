database: 
-   table :  
    + documents(doc_id, title, create_at, last_modified, create_by)
    + documents_by_user(user_id pk,doc_id, title, last_modified)
    + active_users(doc_id , user_id, username, last_seen)
    + operations(doc_id pk, timestamp, op_id, user_id, payload)
    + document_locks(doc_id pk, locked_by, locked_at)